var fs = require('fs');
var express = require('express');
var fileName = "deema.json";

fs.watch(fileName, {
  persistent: true
}, function(event, filename) {
  console.log(event + " event occurred on " + filename);
});